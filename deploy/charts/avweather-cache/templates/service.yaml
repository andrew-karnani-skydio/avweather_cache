apiVersion: v1
kind: Service
metadata:
  name: {{ include "avweather-cache.fullname" . }}
  labels:
    {{- include "avweather-cache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avweather-cache.selectorLabels" . | nindent 4 }}
